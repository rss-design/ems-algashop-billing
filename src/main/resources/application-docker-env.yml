spring:
  datasource:
    url: jdbc:postgresql://algashop-postgres:5432/billing
  jpa:
    show-sql: true
  flyway:
    locations: classpath:db/migration
    user: postgres
    password: postgres
    url: jdbc:postgresql://algashop-postgres:5432/billing

algashop:
  integrations:
    payment:
      fastpay:
        webhook-url: http://algashop-billing:8082/api/v1/webhooks/fastpay
